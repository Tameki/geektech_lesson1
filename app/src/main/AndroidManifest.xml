<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.geektech.navlesson">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_navigation"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_navigation"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".bottomnavbar.BottomNavActivity">

        </activity>

        <activity android:name=".study.MixNavigationActivity">

        </activity>

    </application>

</manifest>